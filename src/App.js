import React from "react";

import Quote from "./components/Quote/Quote";

import classes from "./App.module.css";

const App = () => {
    return <Quote className={classes.Quote} />;
};

export default App;
